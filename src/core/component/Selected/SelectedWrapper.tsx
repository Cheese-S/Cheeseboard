import React from "react"

import { SelectedBound } from "./SelectedBound";

export const SelectedWrapper: React.FC = ({}) => {
    return (
        <React.Fragment>
            <SelectedBound />
        </React.Fragment>
    )
}